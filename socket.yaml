asyncapi: 2.6.0
info:
  title: Kitchen & Order Socket Service (CDTN)
  version: 1.0.5
  description: |
    Tài liệu mô tả giao thức Socket.IO cho hệ thống quản lý nhà hàng.
    
    ### 🔐 Hướng dẫn xác thực (Auth)
    - **Handshake**: Gửi Token qua `auth: { Authorization: "Bearer <token>" }` hoặc Headers.
    - **Permissions**: Server tự động decode token để check quyền (Admin/Guest).

servers:
  dev:
    url: http://localhost:3000
    protocol: socket.io
    description: Development Server
    security:
      - bearerAuth: []

tags:
  - name: Admin Actions
    description: Hành động do Admin/Bếp thực hiện (Client -> Server)
  - name: Guest Actions
    description: Hành động do Khách hàng thực hiện (Client -> Server)
  - name: Server Notifications
    description: Sự kiện Server bắn về (Server -> Client)

channels:
  # ==========================================
  # 1. KHU VỰC ADMIN (Client -> Server)
  # ==========================================
  join_kitchen_room:
    publish:
      tags:
        - name: Admin Actions
      summary: Admin tham gia phòng bếp
      description: |
        Admin join vào `admin_room` để nhận thông báo đơn mới.
        **Quyền:** `socket_join_kitchen`
      message:
        payload: 
          type: object
          nullable: true
    subscribe:
      tags:
        - name: Server Notifications
      summary: Phản hồi Join thành công
      message:
        $ref: '#/components/messages/JoinSuccess'

  update_order_status:admin:
    publish:
      tags:
        - name: Admin Actions
      summary: Admin cập nhật trạng thái món
      description: |
        Chuyển trạng thái: Pending -> Cooking -> Served -> Reject.
        Server sẽ bắn `update_order_item` và `order_update:admin` về cho client.
      message:
        payload:
          $ref: '#/components/schemas/UpdateStatusInput'
      bindings:
        socketio:
          ack:
            $ref: '#/components/schemas/StandardAck'

  create_order:admin:
    publish:
      tags:
        - name: Admin Actions
      summary: Admin tạo đơn hộ khách
      description: Tạo hoặc cập nhật đơn cho một bàn cụ thể.
      message:
        payload:
          $ref: '#/components/schemas/CreateOrderInput'
      bindings:
        socketio:
          ack:
            $ref: '#/components/schemas/StandardAck'

  # ==========================================
  # 2. KHU VỰC GUEST (Client -> Server)
  # ==========================================
  join_table:
    publish:
      tags:
        - name: Guest Actions
      summary: Khách join vào bàn ăn
      description: |
        Join vào room `table_{tableId}` để nhận thông báo realtime của bàn.
        **Quyền:** `socket_join_own_table`
      message:
        payload:
          type: object
          properties:
            tableId:
              type: string
              example: "6578a9b1c2d3e4f5a6b7c8d9"

  create_order:guest:
    publish:
      tags:
        - name: Guest Actions
      summary: Khách gọi món
      description: Gửi danh sách món lên server.
      message:
        payload:
          $ref: '#/components/schemas/CreateOrderInput'
      bindings:
        socketio:
          ack:
            $ref: '#/components/schemas/StandardAck'

  cancel_order:guest:
    publish:
      tags:
        - name: Guest Actions
      summary: Khách hủy món
      description: |
        Khách hủy món khi đang ở trạng thái Pending.
        Server hoàn kho, trừ tiền và trả về thông tin cập nhật.
      message:
        payload:
          type: object
          required: [orderId, itemId, status]
          properties:
            orderId:
              type: string
              example: "6578a9b1c2d3e4f5a6b7c8d9"
            itemId:
              type: string
              example: "6578a9b1c2d3e4f5a6b7c999"
            status:
              type: string
              enum: [Reject]
              example: "Reject"
      bindings:
        socketio:
          ack:
            type: object
            properties:
              success:
                type: boolean
              message:
                type: string
              data:
                type: object
                description: Data trả về trong callback (chứa tiền mới)
                properties:
                  orderId:
                    type: string
                  itemId:
                    type: string
                  status: 
                    type: string
                  mamageBy:
                    type: string
                  newTotalAmount:
                    type: number
                    description: Tổng tiền mới sau khi trừ hoàn tiền
                  message:
                    type: string
                    description: Thông báo chi tiết

  get_orders:guest:
    publish:
      tags:
        - name: Guest Actions
      summary: Lấy danh sách món (Đã phân nhóm)
      description: |
        Dùng khi khách mở popup "Theo dõi đơn hàng".
        Server trả về danh sách items đã được chia vào 4 key: Pending, Cooking, Served, Reject.
      message:
        payload:
          type: object
          properties:
            tableId:
              type: string
      bindings:
        socketio:
          ack:
            type: object
            properties:
              success:
                type: boolean
                example: true
              message:
                type: string
                example: "Lấy danh sách đơn hàng thành công"
              data:
                $ref: '#/components/schemas/GroupedOrderItems'

  # ==========================================
  # 3. SERVER NOTIFICATIONS (Server -> Client)
  # ==========================================
  
  # Notification: update_order_item
  update_order_item:
    subscribe:
      tags:
        - name: Server Notifications
      summary: Thông báo trạng thái món thay đổi (Toast)
      description: >
        Gửi tới `admin_room` và `table_{id}`.
        Dùng để hiện Toast: "Món X đã chuyển sang Y".
      message:
        payload:
          type: object
          properties:
            orderId:
              type: string
            itemId:
              type: string
            status:
              type: string
              enum: [Pending, Cooking, Served, Reject]
            mamageBy:
              type: string
            newTotalAmount:
              type: number
            message:
              type: string

  # Notification: order_update:admin
  order_update:admin:
    subscribe:
      tags:
        - name: Server Notifications
      summary: Cập nhật chi tiết đơn hàng (Admin)
      description: Gửi tới `admin_room` mỗi khi có thay đổi để reload danh sách.
      message:
        payload:
          type: object
          properties:
            order:
              type: array
              items:
                $ref: '#/components/schemas/OrderObject'

  # Notification: order:update
  order:update:
    subscribe:
      tags:
        - name: Server Notifications
      summary: Gửi về TOÀN BỘ object đơn hàng mới (Guest)
      description: Gửi tới `table_{id}` để đồng bộ full data.
      message:
        payload:
          $ref: '#/components/schemas/OrderObject'

  # Notification: New Order (Admin)
  new_order:admin:
    subscribe:
      tags:
        - name: Server Notifications
      summary: Có đơn mới (Gửi cho Admin)
      message:
        payload:
          type: object
          properties:
            type: 
              type: string
              example: "NEW_ORDER_CREATED:ADMIN"
            message:
              type: string
            data:
              $ref: '#/components/schemas/OrderObject'

  # Notification: New Order (Guest)
  new_order:guest:
    subscribe:
      tags:
        - name: Server Notifications
      summary: Có đơn mới (Gửi cho Khách cùng bàn)
      message:
        payload:
          type: object
          properties:
            type: 
              type: string
              example: "NEW_ORDER_CREATED:CLIENT"
            message:
              type: string
            data:
              $ref: '#/components/schemas/OrderObject'

  # [NEW] Notification: Thanh toán thành công (Cho Khách)
  payment_success:
    subscribe:
      tags:
        - name: Server Notifications
      summary: Thông báo thanh toán thành công (Guest)
      description: |
        Gửi tới `table_{id}` khi Webhook SePay xác nhận tiền đã về.
        FE dùng sự kiện này để đóng modal QR và hiện màn hình cảm ơn.
      message:
        payload:
          $ref: '#/components/schemas/PaymentSuccessGuestPayload'

  # [NEW] Notification: Thanh toán thành công (Cho Admin)
  payment_received:
    subscribe:
      tags:
        - name: Server Notifications
      summary: Thông báo nhận tiền (Admin)
      description: |
        Gửi tới `admin_room` khi có bàn thanh toán.
        Giúp thu ngân biết tiền đã về mà không cần check app ngân hàng.
      message:
        payload:
          $ref: '#/components/schemas/PaymentReceivedAdminPayload'

  error_msg:
    subscribe:
      tags:
        - name: Server Notifications
      summary: Thông báo lỗi chung
      message:
        payload:
          type: string

# ==========================================
# DEFINITIONS
# ==========================================
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    StandardAck:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string

    UpdateStatusInput:
      type: object
      required: [orderId, itemId, status]
      properties:
        orderId:
          type: string
        itemId:
          type: string
        status:
          type: string
          enum: [Pending, Cooking, Served, Reject]

    CreateOrderInput:
      type: object
      required: [tableId, guestName, items]
      properties:
        tableId:
          type: string
        guestName:
          type: string
        items:
          type: array
          items:
            type: object
            properties:
              dishId: 
                type: string
              quantity: 
                type: integer
              note: 
                type: string

    DishItemObject:
      type: object
      properties:
        _id:
          type: string
        dishId:
          type: string
        dishName:
          type: string
        dishPrice:
          type: number
        dishImage:
          type: string
        quantity:
          type: integer
        note:
          type: string
        orderedBy:
          type: string
        status:
          type: string
          enum: [Pending, Cooking, Served, Reject]
        createdAt:
          type: string
          format: date-time

    OrderObject:
      type: object
      properties:
        _id:
          type: string
        tableNumber:
          type: integer
        status:
          type: string
        totalAmount:
          type: number
        items:
          type: array
          items:
            $ref: '#/components/schemas/DishItemObject'

    GroupedOrderItems:
      type: object
      properties:
        Pending:
          type: array
          items:
            $ref: '#/components/schemas/DishItemObject'
        Cooking:
          type: array
          items:
            $ref: '#/components/schemas/DishItemObject'
        Served:
          type: array
          items:
            $ref: '#/components/schemas/DishItemObject'
        Reject:
          type: array
          items:
            $ref: '#/components/schemas/DishItemObject'

    # [NEW] Payload cho sự kiện payment_success (Guest)
    PaymentSuccessGuestPayload:
      type: object
      properties:
        orderId:
          type: string
          example: "693f7a63e7455fa6893e7080"
        amount:
          type: number
          example: 266000
        message:
          type: string
          example: "Thanh toán thành công!"

    # [NEW] Payload cho sự kiện payment_received (Admin)
    PaymentReceivedAdminPayload:
      type: object
      properties:
        orderId:
          type: string
          example: "693f7a63e7455fa6893e7080"
        tableNumber:
          type: integer
          example: 5
        amount:
          type: number
          example: 266000
        message:
          type: string
          example: "Bàn 5 vừa thanh toán 266,000đ"

  messages:
    JoinSuccess:
      payload:
        type: object
        properties:
          message:
            type: string
          roomId:
            type: string